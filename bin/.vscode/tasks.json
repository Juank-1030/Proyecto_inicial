{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "javac",
            "args": [
                "-source", "21",
                "-target", "21",
                "-d", "${workspaceFolder}/bin",
                "shapes/*.java",
                "&&",
                "javac",
                "-source", "21",
                "-target", "21",
                "-d", "${workspaceFolder}/bin",
                "-cp", "${workspaceFolder}/bin",
                "silkroad/*.java"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
